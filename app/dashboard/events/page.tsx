import PlaceholderPage from '@/components/layout/PlaceholderPage';
import { Calendar } from 'lucide-react';

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Événements"
      description="Calendrier et planification des événements"
      icon={Calendar}
      color="blue"
    />
  );
}

import PlaceholderPage from '@/components/layout/PlaceholderPage';
import { Building } from 'lucide-react';

export default function UnitsPage() {
  return (
    <PlaceholderPage
      title="Unités"
      description="Organisation en unités"
      icon={Building}
      color="blue"
    />
  );
}
